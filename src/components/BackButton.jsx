import { 
    Button, 
    styled
  } from "@mui/material"
const BackButton = styled(Button)({
    fontFamily: 'RobotoMono',
    color: '#505862',
    textTransform: 'none',
  })
export default BackButton